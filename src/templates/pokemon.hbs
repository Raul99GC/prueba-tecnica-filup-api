<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>

     * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      overflow-x: hidden;
      font-size: 62.5%;
      /* 1 rem === 10px */
    }

    body {
      font-size: 16px;
      margin: 20px;
      font-family: 'santander';
    }

    .flex {
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .pageCard {
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .pageCardBox {
      width: 75%;
      height: 90%;
      flex-direction: column;
      border: .2rem solid #4b4b4b;
      border-radius: 1.6rem;
      position: relative;
      gap: 2rem;
      margin: auto;
    }

    .pageCardContentImg {
      width: 120%;
    }

    .pageCardName {
      padding: 1rem;
      text-transform: capitalize;
      color: var(--white);
      letter-spacing: .4rem;
    }

    .pageCardH2Name {
      font-size: var(--h2-big-font);
      text-align: center;
    }

    .pageCardStats {
      display: flex;
      padding: 1rem;
      gap: 1rem;
      text-align: center;

    }

    .pageCardP {
      color: var(--white);
      letter-spacing: .4rem;
    }

    .pageCardTypes {
      width: 100%;
      gap: 2rem;
      padding: 1rem;
    }

    .pageCardStatsPoints {
      width: 80%;
      gap: 2rem;
      flex-wrap: wrap;
      padding-bottom: 2rem;
    }

    .pageCardBoxPointStat {
      flex-direction: column;
      min-width: 8.5rem;
      /* min-height: 6rem; */
      background-color: var(--white);
      box-shadow: 4px 4px 24px rgba(1, 17, 38, 0.2);
      border-radius: 8px;
    }

    .pageCardBoxPoint {
      width: 3.6rem;
      height: 3.6rem;
      border: 3px solid #212121;
      border-radius: 50%;
      text-align: center;
      margin-top: 1rem;
    }

  </style>
  <title>Document</title>
</head>

<body>
  <aside class="pageCard">
    <section class="pageCardBox flex">
      <div class="pageCardContentImg">
        <img class="pageCardImg" src="{{img}}" alt="" />
      </div>

      <div class="pageCardName">
        <h2 class="pageCardH2Name">{{name}}</h2>
      </div>

      <div class="pageCardStats">
        <progress min="0" max="150" value="{{hp}}"></progress>
        <p class="pageCardP">Hp: {{hp}}</p>

        <progress min="0" max="1000" value="{{experience}}"></progress>
        <p class="pageCardP">Experience: {{experience}}</p>
      </div>

      <div class="pageCardTypes flex">
        <span>{{type}}</span>
      </div>
      <div class="pageCardStatsPoints flex">
        {{#each stats}}
        <div class="pageCardBoxPointStat flex">
          <div class="pageCardBoxPoint flex">
            <p class="pageCardPoint">{{this.base_stat}}</p>
          </div>
          <p class="pageCardNamePoint">{{this.stat.name}}</p>
        </div>
        {{/each}}
      </div>
    </section>
</body>

</html>